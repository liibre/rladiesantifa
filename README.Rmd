---
output: github_document
---

# Code to create Rladies ANTIFA hexsticker

You will need to have installed and loaded the following libraries:

```{r, message=FALSE}
# libraries
library(hexSticker)
library(png)
library(grid)
library(plotrix)
library(grDevices)
library(magick)
```

In addition, load these functions

```{r}
source("fct/antifa.R")
```


Here, we define the dark purple from RLadies

```{r}
#colors
rladies <- "#562457" #rladies purple, for a lighter option use: #88398A both from rladies color palete
```

For the ANTIFA flag we use the public domain icon from [The Noun Project](https://thenounproject.com/search/?q=antifa&i=1245689). The function `antifa()` creates the flag with the desired title.

```{r, fig.height = 6, fig.width = 6}
#antifa image
antifa(text.size = 2.3)
antifa(x = "BIÓLOGAS", bg.col = "darkgreen", text.size = 2.3)
antifa(x = "r-ladies+", bg.col = rladies, text.size = 2.3)
```

To create the hexSticker the image should be on disk (`save = TRUE`)

```{r}
antifa(save = TRUE)
sticker(antifa(save = TRUE),
        package = "",
        asp = 0.85,
        s_x = 0.95, s_y = 0.95,
        s_width = 1.2, s_height = 1.2,
        p_size = 13,
        h_fill = rladies, #bg color for hexsticker
        h_color = "black",
        filename = "hexsticker/rladies_antifa_red.png")
```


![](hexsticker/rladies_antifa_red.png)

```{r dark purle, echo = FALSE}
sticker(antifa(bg.col = rladies, text.size = 3, save = TRUE),
        package = "",
        asp = 0.85,
        s_x = 0.95, s_y = 0.95,
        s_width = 1.2, s_height = 1.2,
        p_size = 13,
        h_fill = rladies, #bg color for hexsticker
        h_color = "black",
        filename = "hexsticker/rladies_antifa_purple.png")
```


```{r light purle, echo = FALSE}
sticker(antifa(bg.col = "#88398A", text.size = 3.5, save = TRUE),
        package = "",
        asp = 0.85,
        s_x = 0.95, s_y = 0.95,
        s_width = 1.2, s_height = 1.2,
        p_size = 13,
        h_fill = "#88398A", #bg color for hexsticker
        h_color = "black",
        filename = "hexsticker/rladies_antifa_light_purple.png")
```

